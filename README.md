# installation

```bash
git clone https://github.com/halocxent/last-letter-macro.git && cd last-letter-macro
```

# dependencies 

```bash
pip install pydirectinput pygetwindow
```

Run: `python3 macro.py`
